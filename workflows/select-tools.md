# Workflow: Select Tools

Use `skills/tool-router/SKILL.md`.

1. Read project requirements.
2. Convert external requirements into actions.
3. Check Direct API options.
4. Check MCP options.
5. Check browser automation only if needed.
6. Check Computer Use only as final fallback.
7. Define permissions, failure behavior, and fallback.
8. Update `docs/TOOLS.md`.
9. Update `docs/ENV_VARS.md` with names only.
10. Record important decisions.

Return the tool plan and unresolved permission requirements.
