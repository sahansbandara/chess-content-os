# Python Rules

- Use virtual environments.
- Keep modules small.
- Add type hints where practical.
- Separate config from code.
- Never commit `.env`.
- Add requirements or pyproject files when needed.
