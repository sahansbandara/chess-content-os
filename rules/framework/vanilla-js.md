# Vanilla JS Rules

- Use ES modules.
- Keep DOM selectors centralized where possible.
- Avoid global variables.
- Separate services, utilities, and UI components.
- Use event delegation when useful.
- Validate form input before API calls.
